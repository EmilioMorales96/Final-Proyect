# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@ep-xyz-123.us-east-1.aws.neon.tech/database_name?sslmode=require
DB_NAME=database_name
DB_USER=username
DB_PASSWORD=password
DB_HOST=ep-xyz-123.us-east-1.aws.neon.tech
DB_PORT=5432

# JWT Secret
JWT_SECRET=your_super_secret_jwt_key

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Salesforce Integration
SALESFORCE_INSTANCE_URL=https://your-domain.salesforce.com
SALESFORCE_CLIENT_ID=your_connected_app_client_id
SALESFORCE_CLIENT_SECRET=your_connected_app_client_secret

# Power Automate Integration - OneDrive
ONEDRIVE_ACCESS_TOKEN=your_onedrive_access_token

# Power Automate Integration - Dropbox (alternative)
DROPBOX_ACCESS_TOKEN=your_dropbox_access_token

# Server Configuration (Render)
PORT=3000
NODE_ENV=production

# Render specific notes:
# - Set these variables in Render dashboard
# - DATABASE_URL is typically provided automatically by Neon
# - Make sure to enable "Auto-Deploy" for continuous deployment
